import 'package:flutter/material.dart';

/// [AppColors] is a class that defines color constants used in the application.
class AppColors {
  // Dark mode colors
  static const steamGreen = Color(0xffB6F09C);
  static const white = Colors.white;
  static const black = Colors.black;
  static const greyShimmerDark = Color(0xFF797979);

  // Light mode colors
  static const grey = Color(0xff555555);
  static const green = Color(0xff28A745);
  static const greyShimmerLight = Color(0xFFACABAB);
  static const greyHighlight = Color(0xFFFAFAFA);
}
